export class Database {
  database = {}

  select(table) // tabela que quero selecionar, e ele vai retornar todos os dados contido dentro desta tabela.
 
  insert(table, data) // Esse insert vai receber a tabela do banco que eu qeuro fazer inserção e, os dados também.
}